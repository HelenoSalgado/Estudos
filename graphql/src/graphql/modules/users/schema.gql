type User {
    id: ID!
    name: String
    email: String!
}

type Query {
    users: [User]!
    user(id: ID!): User!
}

input UserInput{
    name: String
    email: String
}

type Mutation {
    createUser(data: UserInput): User!
    updateUser(id: ID!, data: UserInput): User!
}